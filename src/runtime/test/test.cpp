#define __TEST
#include <windows.h>
#include <cassert>
#include <iostream>
//#include "test/rt_test.hpp"

int main() {
	/*
	RegInit();
	// a simple of (a + b) * 2
	FObject *fObj = MakeVM();
	Dpp_Object *a = mkConst<IntObject, Interger>(INT_TYPE, 114);
	Dpp_Object *b = mkConst<IntObject, Interger>(INT_TYPE, 514);
	Dpp_Object *const_two = mkConst<IntObject, Interger>(INT_TYPE, 2);
	struct VMState main_state;
	
	Tmp_Heap *_first_theap = new Tmp_Heap;
	_first_theap->PushData({0});
	_first_theap->PushData({1});
	_first_theap->PushData({3});
	OpCode op1 = {OPCODE_ADD, NO_FLAG, _first_theap->getContainer()};
	main_state.vmopcodes.PushData(op1);
	
	Tmp_Heap *_second_theap = new Tmp_Heap;
	_second_theap->PushData({3});
	_second_theap->PushData({2});
	_second_theap->PushData({4});
	OpCode op2 = {OPCODE_MUL, NO_FLAG, _second_theap->getContainer()};
	main_state.vmopcodes.PushData(op2);
	
	fObj->obj_map.write(0, a);
	fObj->obj_map.write(1, b);
	fObj->obj_map.write(2, const_two);
	fObj->state = main_state;
	std::cout << "start vm..." << std::endl;
	VM_Run(fObj);*/
}
