native("io", "out") out(object o) => void;

main() => void {
    int i = 0;
    while(i < 10) {
        with if (i == 5) {
            break;
        }

        out(i);
        ++i;
    }

    out(" ");

    int i2 = 0;
    while (i2 < 10) {
        ++i2;
        with if (i2 == 5) {
            continue;
        }

        out(i2);

    }

   int i3 = 0;
    do {
        out(i3);
        ++i3;
    } while (i3 < 0);
}