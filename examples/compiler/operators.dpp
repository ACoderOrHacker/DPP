native("io", "out") out(object o) => void;

int j = 1;
int a = ~j;
bool c = true;
bool b = !c;
int d = ++j;
int e = --j;

main() => void {
    out(a);
    out(" ");
    out(b);
    out(" ");
    out(d);
    out(" ");
    out(e);
}