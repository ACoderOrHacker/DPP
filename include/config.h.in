/// D++ CONFIGURE FILE


#define DXX_VERSION "${VERSION}"
#define DXX_VERSION_MAJOR ${VERSION_MAJOR}
#define DXX_VERSION_MINOR ${VERSION_MINOR}
#define DXX_VERSION_PATCH ${VERSION_ALTER}
#define DXX_PLAT "${plat}"
#define DXX_ARCH "${arch}"
#define DXX_BUILD_MODE "${mode}"
#define DXX_OS "${os}"
#define GIT_COMMIT      "${GIT_COMMIT}"
#define GIT_COMMIT_DATE "${GIT_COMMIT_DATE}"
#define GIT_BRANCH      "${GIT_BRANCH}"
#define GIT_TAG         "${GIT_TAG}"
